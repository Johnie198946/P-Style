# 如何运行 Gemini 3.0 完整功能测试

## 📋 当前状态

✅ **测试脚本已就绪**
- `server_py/scripts/test_with_images.py` - 支持本地文件和 URL
- 支持 Part1、Part2 和完整流程测试

## 🎯 运行测试

### 方法 1：使用本地图片文件

如果您有图片文件在本地，请提供完整路径：

```bash
cd /Users/dengzhaoyu/Desktop/TepVis/AI产品/P-Style/server_py

python3 scripts/test_with_images.py \
  --source "/完整路径/图1.jpg" \
  --target "/完整路径/图2.jpg" \
  --part full \
  --thinking-level high
```

### 方法 2：使用图片 URL

如果图片在网络上，可以直接使用 URL：

```bash
cd /Users/dengzhaoyu/Desktop/TepVis/AI产品/P-Style/server_py

python3 scripts/test_with_images.py \
  --source "https://example.com/source.jpg" \
  --target "https://example.com/target.jpg" \
  --part full \
  --thinking-level high
```

### 方法 3：将图片放到测试目录

1. 将图片复制到 `server_py/test_images/` 目录
2. 运行测试：

```bash
cd /Users/dengzhaoyu/Desktop/TepVis/AI产品/P-Style/server_py

python3 scripts/test_with_images.py \
  --source "test_images/图1.jpg" \
  --target "test_images/图2.jpg" \
  --part full
```

## 📝 测试选项

### 测试部分选择

- `--part part1` - 只测试 Part1（包括可行性评估）
- `--part part2` - 只测试 Part2（需要先有 Part1 结果）
- `--part full` - 完整测试（Part1 + Part2）

### 思考水平选择

- `--thinking-level high` - 高思考水平（默认，适合复杂任务）
- `--thinking-level low` - 低思考水平（更快响应）

## 📊 测试输出

测试完成后会：

1. **控制台输出**：
   - 详细的测试日志
   - 每个步骤的耗时
   - 结果验证信息
   - 测试总结

2. **测试报告**：
   - 保存到：`server_py/test_reports/part1_part2_test_report.json`
   - 包含完整的测试结果和时延数据

## ⚠️ 注意事项

1. **图片格式**：支持 JPG、PNG、WebP 等
2. **图片大小**：建议 < 10MB
3. **API Key**：确保已配置 `GEMINI_API_KEY`
4. **网络**：如果使用 URL，确保网络正常
5. **成本**：每次测试会产生 API 调用费用

## 🚀 快速开始

**请提供图片文件路径，我将立即运行测试！**

例如：
- 图1（参考图-现代建筑）的路径
- 图2（用户图-动漫风格）的路径

或者，如果您想自己运行，使用上面的命令即可。

