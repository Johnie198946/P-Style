# Gemini 3.0 测试执行指南

## 📋 当前状态

✅ **代码迁移完成**
- 模型已更新为 `gemini-3-pro-preview`
- 测试脚本已就绪

✅ **基础测试通过**
- API 调用测试通过
- 时延测试完成

## 🎯 使用提供的图片进行测试

您提到：
- **图1（参考图）**：现代多楼层建筑，干净的功能性设计
- **图2（用户图）**：动漫风格插图，两个年轻人在夜空下

### 方法 1：如果图片文件在本地

```bash
cd /Users/dengzhaoyu/Desktop/TepVis/AI产品/P-Style/server_py

# 运行完整测试（Part1 + Part2）
python3 scripts/test_with_images.py \
  --source /path/to/图1.jpg \
  --target /path/to/图2.jpg \
  --part full \
  --thinking-level high
```

### 方法 2：如果图片是 URL

```bash
cd /Users/dengzhaoyu/Desktop/TepVis/AI产品/P-Style/server_py

# 运行完整测试
python3 scripts/test_with_images.py \
  --source "https://example.com/图1.jpg" \
  --target "https://example.com/图2.jpg" \
  --part full \
  --thinking-level high
```

### 方法 3：只测试 Part1

```bash
python3 scripts/test_with_images.py \
  --source /path/to/图1.jpg \
  --target /path/to/图2.jpg \
  --part part1 \
  --thinking-level high
```

### 方法 4：只测试 Part2

```bash
python3 scripts/test_with_images.py \
  --source /path/to/图1.jpg \
  --target /path/to/图2.jpg \
  --part part2 \
  --thinking-level high
```

## 📊 测试内容

### Part1 测试包括：
1. ✅ 可行性评估（CV 算法）
2. ✅ Gemini API 调用（gemini-3-pro-preview）
3. ✅ JSON 解析和格式化
4. ✅ 结果结构验证
5. ✅ 时延测量

### Part2 测试包括：
1. ✅ Gemini API 调用（gemini-3-pro-preview）
2. ✅ JSON 解析和格式化
3. ✅ 结果结构验证（Lightroom、Photoshop、Color）
4. ✅ 时延测量

## 📝 测试输出

测试完成后会：
1. 在控制台显示详细日志
2. 保存测试报告到：`server_py/test_reports/part1_part2_test_report.json`
3. 显示测试总结（通过/失败、耗时等）

## ⚠️ 注意事项

1. **图片格式**：支持 JPG、PNG、WebP 等常见格式
2. **图片大小**：建议 < 10MB，避免超时
3. **网络**：如果使用 URL，确保网络连接正常
4. **API Key**：确保 `.env` 文件中配置了 `GEMINI_API_KEY`
5. **成本**：每次测试会产生 API 调用费用

## 🚀 快速开始

请提供图片文件的路径，然后运行：

```bash
cd server_py
python3 scripts/test_with_images.py \
  --source <图1路径> \
  --target <图2路径> \
  --part full
```

---

**请提供图片文件路径，我将帮您运行完整测试！**

